services:
  - type: web
    name: lwando-devblog-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 5000
